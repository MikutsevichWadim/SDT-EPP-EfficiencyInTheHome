from scenes.levels.lvl3.objects.stove import Stove



class Stove1(Stove):
    def __init__(self, scene:object):

        pos = [1272, 644]

        super().__init__(
            scene = scene,
            pos = pos,
        )

class Stove2(Stove):
    def __init__(self, scene:object):

        pos = [1290, 628]

        super().__init__(
            scene = scene,
            pos = pos,
        )
        
class Stove3(Stove):
    def __init__(self, scene:object):

        pos = [1417, 633]

        super().__init__(
            scene = scene,
            pos = pos,
        )
        
class Stove4(Stove):
    def __init__(self, scene:object):

        pos = [1406, 650]

        super().__init__(
            scene = scene,
            pos = pos,
        )
        